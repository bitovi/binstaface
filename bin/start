#!/usr/bin/env node
const binstaface = require('../lib');
const path = require('path');

binstaface(path.join(__dirname, '..')).listen(3030, function() {
  console.log(`Server listening on port ${3030}`);
});
